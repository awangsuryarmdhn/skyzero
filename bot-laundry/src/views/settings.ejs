<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings - Sky Laundry Bot</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 font-sans">
    <div class="flex h-screen overflow-hidden">
        <!-- Sidebar -->
        <aside class="w-64 bg-indigo-900 text-white flex flex-col">
            <div class="p-6 text-center border-b border-indigo-800">
                <h1 class="text-2xl font-bold">☁️ Sky Bot</h1>
                <p class="text-xs text-indigo-300 mt-1">Laundry Assistant</p>
            </div>
            <nav class="flex-1 p-4 space-y-2">
                <a href="/admin/dashboard" class="block py-2.5 px-4 rounded hover:bg-indigo-800 transition duration-200">
                    <i class="fas fa-tachometer-alt mr-2"></i> Dashboard
                </a>
                <a href="/admin/settings" class="block py-2.5 px-4 rounded bg-indigo-800 transition duration-200">
                    <i class="fas fa-cog mr-2"></i> Settings
                </a>
                <a href="/admin/logout" class="block py-2.5 px-4 rounded hover:bg-red-600 transition duration-200 mt-8">
                    <i class="fas fa-sign-out-alt mr-2"></i> Logout
                </a>
            </nav>
            <div class="p-4 border-t border-indigo-800">
                <p class="text-xs text-indigo-400 text-center">&copy; 2026 Sky Laundry</p>
            </div>
        </aside>

        <!-- Main Content -->
        <div class="flex-1 flex flex-col overflow-hidden">
            <!-- Header -->
            <header class="bg-white shadow-sm z-10 p-4 flex justify-between items-center">
                <h2 class="text-xl font-semibold text-gray-800">Bot Settings</h2>
                <div class="flex items-center space-x-4">
                    <span class="text-sm text-gray-600">Hallo, <b><%= user && user.name ? user.name : 'Admin' %></b></span>
                    <div class="h-8 w-8 rounded-full bg-indigo-500 flex items-center justify-center text-white">
                        <%= user && user.name ? user.name.charAt(0) : 'A' %>
                    </div>
                </div>
            </header>

            <!-- Content -->
            <main class="flex-1 overflow-x-hidden overflow-y-auto bg-gray-100 p-6">
                
                <div class="max-w-4xl mx-auto">
                    <div class="bg-white rounded-lg shadow-md p-6">
                        <h3 class="text-lg font-semibold text-gray-700 mb-4 border-b pb-2">Konfigurasi Umum</h3>
                        
                        <form action="/admin/settings" method="POST">
                            <!-- Owner Number -->
                            <div class="mb-6">
                                <label for="ownerNumber" class="block text-sm font-medium text-gray-700 mb-1">Nomor Owner (WhatsApp)</label>
                                <input type="text" id="ownerNumber" name="ownerNumber" value="<%= settings.ownerNumber %>" 
                                    class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
                                    placeholder="Contoh: 628123456789">
                                <p class="text-xs text-gray-500 mt-1">Nomor ini yang berhak mengakses menu LAPORAN/OMSET.</p>
                            </div>

                            <!-- Auto Reply Toggle -->
                            <div class="mb-6 flex items-center">
                                <input type="checkbox" id="autoReply" name="autoReply" <%= settings.autoReply ? 'checked' : '' %>
                                    class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded">
                                <label for="autoReply" class="ml-2 block text-sm text-gray-900">
                                    Aktifkan Smart Auto-Reply (Harga, Alamat, Jam Buka)
                                </label>
                            </div>

                            <div class="flex justify-end">
                                <button type="submit" class="bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                                    <i class="fas fa-save mr-2"></i> Simpan Perubahan
                                </button>
                            </div>
                        </form>
                    </div>

                    <!-- Info Box -->
                    <div class="mt-6 bg-blue-50 border-l-4 border-blue-500 p-4">
                        <div class="flex">
                            <div class="flex-shrink-0">
                                <i class="fas fa-info-circle text-blue-500"></i>
                            </div>
                            <div class="ml-3">
                                <p class="text-sm text-blue-700">
                                    Pengaturan ini tersimpan di database. Perubahan akan langsung efektif tanpa perlu restart bot (kecuali terjadi error koneksi).
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

            </main>
        </div>
    </div>
</body>
</html>
